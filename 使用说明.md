# MOD 管理器 v1.2.0 使用说明

一个中文图形界面的游戏 MOD 管理工具：安装、批量导入、启用/禁用、排序、检查、一键部署到游戏，并且随时能还原。

单文件绿色版（`MOD管理器.exe`，约 145 KB），双击即用，**不需要安装**，不写注册表，也不修改游戏本体文件。

支持两种部署方式：

| 模式 | 适用 | 行为 |
| --- | --- | --- |
| **Mods 子目录（星露谷）** | 星露谷 / SMAPI，以及所有「每个 MOD 一个文件夹」的游戏 | 把每个启用的 MOD 整个文件夹复制到 `游戏目录\Mods\` |
| **游戏根目录（通用）** | 其他单机游戏（贴图替换、模型替换等） | 按加载顺序把 MOD 里的文件合并复制到游戏根目录，同名文件按顺序覆盖 |

---

## 一、系统要求

- Windows 10 / Windows 11（32 位、64 位均可）
- 依赖系统自带的 .NET Framework 4.8（Win10 1903 以后、Win11 默认自带）
- 安装 `.7z` / `.rar` 格式需要电脑里装有 7-Zip 或 WinRAR；`.zip` 不需要任何额外程序

---

## 二、星露谷玩家：三分钟上手

程序目录下如果存在 `MOD工作区` 文件夹，启动时会自动打开它（把工作区放在程序旁边即可便携使用）。

1. 双击 `MOD管理器.exe`，会自动打开 `MOD工作区`。
2. **把已有的 MOD 纳管进来**：在左侧列表上点右键 →「导入游戏 Mods 目录中的现有 MOD」，
   程序会把游戏 `Mods\` 里已经装好的 MOD 复制一份到工作区（只复制，不动游戏里的文件）。
3. **以后装新 MOD**：把下载的 `.zip` 直接拖进窗口，或点「＋ 安装 MOD」。装好后是「已启用」状态。
4. **部署进游戏**：确认右上角「部署方式」是 `Mods 子目录（星露谷）`，再点 **「部署到游戏目录」**。
   程序会把每个 MOD 作为一个完整文件夹复制到 `Stardew Valley\Mods\`。
5. **想关掉某个 MOD**：取消勾选即可，然后重新点一次「部署到游戏目录」（或直接点「禁用」后部署）。

> 启动游戏请用 `StardewModdingAPI.exe`（SMAPI）。工具里的「启动游戏」按钮已经指向它。
> 每次部署前，被覆盖的文件夹都会整体备份，点「还原上次部署」就能回到部署前的状态。

---

## 三、通用游戏：三步上手

1. **新建/打开工作区**：程序会在其中建立 `mods`（已启用）/ `disabled`（已禁用）/ `backup`（备份）三个目录。
2. **装 MOD**：点「＋ 安装 MOD」选压缩包，或直接把 `.zip` / `.7z` / `.rar`（含解压好的文件夹）拖进窗口。
3. **部署**：选择「游戏目录」，把「部署方式」切换为 `游戏根目录（通用）`，勾选要用的 MOD，点「部署到游戏目录」。

---

## 四、功能说明

### 安装 MOD

- 支持 `.zip`（内置解压）、`.7z` / `.rar`（调用系统中的 7-Zip / WinRAR），也支持直接拖入文件夹。
- 自动清理 `__MACOSX`、`.DS_Store`、`Thumbs.db` 等垃圾文件。
- 如果压缩包只套了一层外壳文件夹（如 `AwesomeMod\data\...`），会自动去掉外壳；
  遇到 `Mods`、`Content`、`data`、`bin` 等游戏标准目录则保留。
- 同名 MOD 自动改名为 `名字 (2)`，不会覆盖已有的。

### 批量导入（一次装一堆 MOD）

如果你下载了一整包 MOD（或者像 `D:\Downloads\模组工具\宠物\xxx` 这样按分类整理好的文件夹），不用一个一个拖：

1. 点工具栏的 **「批量导入」**，选中那个文件夹（也可以直接把文件夹拖进窗口）；
2. 工具会自动扫描，弹窗告诉你找到多少个 MOD、哪些已经装过、总共多大；
3. 确认后一次性全部复制进工作区。

识别规则：

- 含 `manifest.json` 的文件夹（星露谷 / SMAPI 的 MOD）→ 每个装成一个 MOD，**名字优先用 manifest 里的显示名**；
- 压缩包 `.zip` / `.7z` / `.rar` → 逐个解压安装；
- 层层嵌套的分类文件夹（`宠物\艾尔的马匹\`、`美化\子分类\xx\`）→ 自动递归找出所有 MOD（最多 4 层）；
- 按 `UniqueID` 去重：同一个 MOD 出现两次只会装一次；两个 MOD 显示名相同会用文件夹名区分；
- 如果整个文件夹里既没有 `manifest.json` 也没有压缩包，就把一级子文件夹当作 MOD（给非星露谷游戏用）。

### 启用 / 禁用

- 勾选列表左侧复选框 = 启用，取消勾选 = 禁用（把文件夹在 `mods` / `disabled` 之间移动，不动文件内容）。
- 支持多选批量操作、空格键切换、右键菜单。

### 加载顺序（通用模式才生效）

- 「顺序」列数字越大越靠后加载，同名文件会覆盖前面的 MOD。用「上移 / 下移」调整。

### 检查（原来叫「冲突检测」）

- **Mods 模式**：检查每个已启用 MOD 是否包含 `manifest.json`（SMAPI 的识别标志），
  并列出部署时会覆盖的已有文件夹、目标目录里现有多少个 MOD。
- **通用模式**：扫描所有已启用 MOD，列出重复的文件路径，并指出最终生效的 MOD。

### 部署到游戏目录 / 还原

- **Mods 模式**：`工作区\mods\自动门` → `Stardew Valley\Mods\自动门`（整个文件夹）。
- **通用模式**：`MOD\data\textures\a.dds` → `游戏目录\data\textures\a.dds`（逐文件合并）。
- 覆盖前，被覆盖的文件 / 文件夹会先备份到 `backup\deploy_时间戳\`。
- 点「还原上次部署」：恢复原有文件/文件夹，删除本次新增的，游戏回到部署前状态；连续部署多次也能回到最初状态。

### 备份与恢复

- 删除 MOD 不会真删，而是移动到 `backup\deleted_名字_时间\`，可在「备份与恢复」里找回来（保留删除前的启用状态）。
- 「永久删除」才会从磁盘清除，操作前会二次确认。

### 导入已有 MOD（Mods 模式）

- 右键列表 →「导入游戏 Mods 目录中的现有 MOD」，把游戏 `Mods\` 里已经手动装好的 MOD 复制进工作区统一管理。
- 已存在的同名 MOD 会自动跳过，重复执行不会产生副本。

### 导出 / 导入清单

- 「导出清单」把 MOD 列表、启用状态、顺序、版本/作者/备注存成 json；「导入清单」在另一台电脑上按名称匹配恢复。

### 启动游戏

- 「选择游戏程序」指定主程序（星露谷请选 `StardewModdingAPI.exe`），之后可一键启动，工作目录自动设为游戏目录。

### 快捷键

| 按键 | 作用 |
| --- | --- |
| `F5` | 刷新列表 |
| `空格` | 启用 / 禁用选中的 MOD |
| `Delete` | 删除选中的 MOD（进备份） |
| `Ctrl + A` | 全选 |
| 双击 MOD | 打开该 MOD 的文件夹 |
| 右键 MOD | 完整操作菜单（含导入已有 MOD） |

---

## 五、常见问题

**Q：部署后游戏里没生效？**
① 确认「部署方式」选对了（星露谷必须用 Mods 子目录模式）；
② 确认「游戏目录」指向游戏安装根目录（星露谷是含 `Stardew Valley.exe` 的那一层）；
③ 用 SMAPI 启动游戏（`StardewModdingAPI.exe`），普通启动不会加载 MOD。

**Q：提示「未检测到 7-Zip」，无法安装 7z / rar？**
装一个 [7-Zip](https://www.7-zip.org/)，或先手动解压，再把解压出的**文件夹**拖进本工具。

**Q：会把游戏弄坏吗？**
不会。工具只按你的指令复制文件，且覆盖前先备份，随时可以「还原上次部署」。

**Q：需要管理员权限吗？**
一般不需要。若游戏装在 `C:\Program Files` 等受保护目录，部署被拒时请右键以管理员身份运行。

**Q：怎么彻底卸载？**
删除工具文件夹即可（设置存放在 `%APPDATA%\MODManager`，工作区是你的数据，按需保留）。

---

## 六、开发信息

- C# + WinForms（.NET Framework 4.x），单文件约 135 KB，无第三方依赖。
- 目录结构：`src\`（界面与核心逻辑）、`tests\CoreTests.cs`（自动化测试）、`build\build.ps1`（一键编译）、`MOD工作区\`（预置工作区）。
- 重新编译：

  ```powershell
  powershell -ExecutionPolicy Bypass -File .\build\build.ps1 -ExeName "MOD管理器.exe" -BuildTests
  ```

  产物输出到 `outputs\`，测试程序输出到 `work\CoreTests.exe`（59 项用例，全部通过）。
